# Claude Skill安装故障排除

## ❌ 常见错误：root directory name must contain only alphanumeric characters, underscores, and hyphens

### 错误截图示例

![Claude命名错误](file:///C:/Users/Administrator/.gemini/antigravity/brain/4a3ec391-0d8d-4d52-9d24-8c5d904eaa61/uploaded_media_1769593336777.png)

### 问题原因

Claude对skill根目录名称有严格限制：
- ✅ 允许：字母（a-z, A-Z）
- ✅ 允许：数字（0-9）
- ✅ 允许：下划线（_）
- ✅ 允许：连字符（-）
- ❌ **不允许**：空格
- ❌ **不允许**：其他特殊字符
- ❌ **不允许**：中文字符

### 解决方案

#### 方案1：使用正确命名的zip文件（推荐）

已为您创建符合Claude命名规范的zip文件：

**文件位置**：`g:\zmd works\skills\game-design-doc.zip`

**上传步骤**：
1. 在Claude中点击"Upload skill"
2. 选择 `game-design-doc.zip` 文件
3. 上传成功后，skill名称会是 `game-design-doc`

#### 方案2：重命名文件夹（如果需要）

如果您想修改原始文件夹名称以符合更严格的规范：

```powershell
# 将下划线改为连字符（Claude更推荐）
Rename-Item "game_design_doc" "game-design-doc"
```

**注意**：
- `game_design_doc`（下划线）应该是合法的
- `game-design-doc`（连字符）是更安全的选择
- 避免使用包含空格的路径

#### 方案3：手动创建符合规范的zip

```powershell
# 进入skills目录
cd "g:\zmd works\skills"

# 创建zip（使用连字符命名）
Compress-Archive -Path "game_design_doc" -DestinationPath "game-design-doc.zip" -Force
```

### 验证清单

上传前确认：
- [ ] zip文件名只包含字母、数字、连字符、下划线
- [ ] zip文件名**没有空格**
- [ ] zip文件名**没有中文字符**
- [ ] zip内部包含SKILL.md文件
- [ ] SKILL.md有正确的YAML frontmatter

### 正确的文件结构

```
game-design-doc.zip
└── game_design_doc/          # 或 game-design-doc/
    ├── SKILL.md              # 必需
    ├── README.md
    ├── INSTALL.md
    ├── prompts/
    │   └── expand_doc.md
    ├── scripts/
    │   └── generate_doc.py
    └── examples/
        └── ...
```

### 其他常见问题

#### 问题：上传后提示"missing SKILL.md"

**原因**：zip文件结构不正确，可能多了一层目录

**解决**：
```powershell
# 错误的打包方式（会创建双层目录）
Compress-Archive -Path "." -DestinationPath "skill.zip"

# 正确的打包方式
cd ..
Compress-Archive -Path "game_design_doc" -DestinationPath "game-design-doc.zip"
```

#### 问题：frontmatter格式错误

**检查SKILL.md开头**：
```yaml
---
name: 游戏功能开发文档生成器
description: AI辅助从简要玩法描述生成详细的游戏功能开发文档，指导客户端和服务器程序员实现功能
---
```

确保：
- 三个连字符 `---` 在开头和结尾
- `name` 和 `description` 字段存在
- YAML格式正确（冒号后有空格）
- 文件编码是UTF-8

### Claude vs Antigravity 差异

| 特性 | Claude | Antigravity |
|------|--------|-------------|
| 命名限制 | 更严格 | 较宽松 |
| 安装方式 | 上传zip | 直接复制文件夹 |
| 路径要求 | 不能含空格 | 可以含空格 |
| 自动加载 | 上传后立即可用 | 自动扫描目录 |

### 推荐实践

1. **使用连字符**（-）而非下划线（_），更安全
2. **避免空格**，用连字符或下划线替代
3. **使用纯英文命名**，避免中文
4. **测试zip文件**，上传前先解压验证结构
5. **保留原文件夹**，以防需要重新打包

---

## 🎯 快速修复

**一键命令**（PowerShell）：
```powershell
cd "g:\zmd works\skills"
Compress-Archive -Path "game_design_doc" -DestinationPath "game-design-doc.zip" -Force
```

**然后**：
1. 找到 `g:\zmd works\skills\game-design-doc.zip`
2. 在Claude中上传此文件
3. ✅ 完成！
